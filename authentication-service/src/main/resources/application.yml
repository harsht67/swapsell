server:
  port: 8080
spring:
  application:
    name: authentication-service
  datasource:
    url: jdbc:mysql://localhost:3306/ssuser?createDatabaseIfNotExist=true
    username: root
    password: MySQLpass@67
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
      dialect: org.hibernate.dialect.MYSQL80Dialect
